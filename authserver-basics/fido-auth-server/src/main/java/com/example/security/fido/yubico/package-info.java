/**
 * This package is contains the integration layer between the Yubico Library and Spring. The package does three tings.
 * First it configures the Relying on Party, second it implements the Yubico CredentialRepository Interface. Third, it
 * contains some utility classes for working with data types that are part of the Yubico library.
 *
 *
 */
package com.example.security.fido.yubico;